# Copie para .env no servidor (o script deploy/instalar.sh faz isso por você).

# Seu domínio, sem https:// e sem www.
DOMINIO=seudominio.com.br

# Opcionais: limites para o público (em MB). Os valores abaixo são os padrões.
# MAX_DOC_MB=20
# MAX_VIDEO_MB=100
# ZIP_MAX_MB=100
# LIMITE_UPLOAD=110MB
# RATE_LIMIT_MAX=20
# MAX_ACTIVE_PER_IP=2
